Dropdown select matching Input styling, with a chevron affordance.

```jsx
<Select label="Tipo" options={[{value:'receita',label:'Receita'},{value:'despesa',label:'Despesa'}]} />
```

Pass `options` (strings or `{value,label}`) or raw `<option>` children. Same label/hint/error API as Input.
